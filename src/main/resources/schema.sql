CREATE TABLE car (
    id INT PRIMARY KEY AUTO_INCREMENT,
    carName VARCHAR(255),
    exhaustGas VARCHAR(255),
    cylinderNumber VARCHAR(255),
    maxSpeed VARCHAR(255),
    horsePower VARCHAR(255),
    carWeight VARCHAR(255),
    pictureUrl VARCHAR(255)
);